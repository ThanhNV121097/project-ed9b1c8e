# project-ed9b1c8e
A single page that shows the text "Hello Word" centred on the screen, horizontally and vertically. The text is not hardcoded in the frontend: it is stored in one row in PostgreSQL and the page reads it through the backend API. Plain white background, black text, no palette, no animation. As plain as possible — this exists only to prove the pipeline
