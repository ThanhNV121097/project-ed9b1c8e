# PostgreSQL connection string injected by runtime or compose.
DATABASE_URL=postgres://app:app_secret@localhost:5432/app?sslmode=disable

# HTTP port preferred by runtime.
PORT=8080

# Fallback HTTP port when PORT is unset.
APP_PORT=8080
